#include <iostream>
#include <raylib.h>

int main() {
  InitWindow(600, 600, "C++ - Asteroids");
  while (!WindowShouldClose()) {
    BeginDrawing();

    EndDrawing();
  }
  CloseWindow();
  return 0;
}